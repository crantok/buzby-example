<?php

// GET vars

function has($name) {
    return ! empty( $_GET[$name] );
}

function get($name) {
    if ( has($name) ) return urldecode( htmlspecialchars( $_GET[$name] ) );
}

?>

<h2>Contact us</h2>

<?php if ( has('result-message') ): ?>
<div class="message">
    <div class="<?php print get('result-status'); ?>">
        <p>
            <?php print get('result-message'); ?>
        </p>
    </div>
</div>
<?php endif; ?>

<form name="ContactForm" id="ContactForm" action="/contact.php" method="post">

   <div class="form-group">
        <label for="ContactName">Your name</label>
        <input type="text" class="form-control" id="ContactName" placeholder="Enter your name" name="name" required
               value="<?php print get('name')?>">
    </div>

    <div class="form-group">
        <label for="ContactEmail">Email address</label>
        <input type="email" class="form-control" id="ContactEmail" placeholder="Enter your email address" name="email" required
               value="<?php print get('email')?>">
    </div>

    <div class="form-group">
        <label for="ContactMessage">Your message</label>
        <textarea class="form-control" id="ContactMessage" rows="5" name="message" required
                  ><?php print get('message')?></textarea>
    </div>

    <input type="submit" class="btn btn-default btn-primary pull-right" value="Send message" id="submit"/>

</form>
